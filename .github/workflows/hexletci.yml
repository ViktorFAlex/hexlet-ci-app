name: hexlet-ci
on: push
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: make setup
      - run: make lint
      - run: make test
  log:
    runs-on: ubuntu-latest
    needs: build
    steps: 
    - run: make say-hello
